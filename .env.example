# Frontend uses same-origin /api requests in production.
# Optional local override:
VITE_API_URL=/api

# Worker secrets (set with `wrangler secret put` in deployed environments):
# SESSION_SECRET=<at least 32 random bytes>
