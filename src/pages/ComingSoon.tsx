import{BookOpen,Settings,Sparkles}from'lucide-react';import{PageHeader}from'../components/PageHeader';export default function ComingSoon({type}:{type:'resources'|'settings'}){const resource=type==='resources';return <><PageHeader eyebrow={resource?'Knowledge & formation':'Workspace administration'} title={resource?'Resources with a source.':'Standards that evolve.'} copy={resource?'A trusted home for approved guides, training and ministry direction.':'Configure your local operating standards without rewriting history.'}/><section className="coming-panel">{resource?<BookOpen/>:<Settings/>}<p className="eyebrow"><Sparkles/> Prepared for the next release</p><h2>{resource?'A trusted library is taking shape.':'Your standards engine is next.'}</h2><p>{resource?'Only approved, versioned ministry material will live here—keeping current direction distinct from historical resources.':'Roles, hierarchy labels, reporting rules and approval paths will be configurable and versioned here.'}</p><div className="roadmap"><span><i/>Source registry</span><span><i/>Version history</span><span><i/>Role-scoped publishing</span></div></section></>}
