import{useState,type FormEvent}from'react';import{ArrowRight,Eye,EyeOff,ShieldCheck,Sparkles}from'lucide-react';import{motion}from'motion/react';import{Logo}from'../components/Logo';import{useAuth}from'../AuthContext'
export default function Login(){const{login}=useAuth();const[email,setEmail]=useState('admin@nexus.demo'),[password,setPassword]=useState('Welcome2026!'),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password)}catch(err){setError(err instanceof Error?err.message:'Unable to sign in')}finally{setBusy(false)}}return <main className="login-page"><div className="ambient ambient-one"/><div className="ambient ambient-two"/><nav className="login-nav"><Logo/><span className="system-status"><i/> Systems operational</span></nav><section className="login-story"><motion.div initial={{opacity:0,y:30}} animate={{opacity:1,y:0}} transition={{duration:.8}}><p className="eyebrow"><Sparkles/> Ministry intelligence, reimagined</p><h1>Every cell has<br/>a <em>story.</em><br/>See it grow.</h1><p className="story-copy">One living system for people, leadership, ministry health and multiplication—built to turn faithful records into meaningful action.</p><div className="story-points"><span><b>04</b> Generations mapped</span><span><b>87%</b> Reporting rhythm</span><span><b>01</b> Shared mission</span></div></motion.div></section><motion.section className="login-panel" initial={{opacity:0,x:40}} animate={{opacity:1,x:0}} transition={{duration:.7,delay:.15}}><div className="panel-glow"/><p className="eyebrow">Secure command access</p><h2>Welcome back.</h2><p>Step into your ministry’s living story.</p><form onSubmit={submit}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><label>Password<span className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required minLength={8}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'}>{show?<EyeOff/>:<Eye/>}</button></span></label>{error&&<div className="form-error" role="alert">{error}</div>}<button className="button primary login-button" disabled={busy}>{busy?'Opening your command center…':<>Enter command center <ArrowRight/></>}</button></form><div className="demo-note"><ShieldCheck/><span><b>Preview access</b><small>Credentials are prefilled for this demonstration.</small></span></div><footer>Protected by encrypted, role-scoped access</footer></motion.section></main>}
